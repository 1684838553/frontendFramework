import React from 'react';
import { Route, Switch, NavLink } from 'dva/router';
import { routes } from '@/models/routes';

const SiderBar: React.FC = (): React.ReactElement => (
  <div className="app">
    
  </div>
);
